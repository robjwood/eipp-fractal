{{# accordion }}
  <div class="c-accordion {{ bgClass }}">
    <img class="c-accordion__icon" src="{{ icon }}" alt="">
    <p class="c-accordion__title  {{ titleClass }}">{{ title }}</p>
    <img class="c-accordion__control" src="{{ control }}" alt="" /> 
  </div>
{{/ accordion }}